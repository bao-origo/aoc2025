#!/usr/bin/env ruby
require_relative 'aoc'

dial = 50
puts (AOC.input(day=1).each_line.reduce(0) do |count, line|
  spins = line[1..].to_i
  dial = (line[0] == 'L' ? dial - spins : dial + spins) % 100
  count + (dial == 0 ? 1 : 0)
end)
