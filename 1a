#!/usr/bin/env ruby
require_relative 'aoc'

dial = 50
count = 0
AOC.input(day=1).each_line do |line|
  spins = line[1..].to_i
  case line[0]
  when 'L'
    dial = (dial - spins) % 100
  when 'R'
    dial = (dial + spins) % 100
  end
  count += 1 if dial == 0
end
puts count
