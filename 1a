#!/usr/bin/env ruby
require_relative 'aoc'

dial = 50
count = 0
AOC.input(day=1).each_line do |line|
  spins = line[1..].to_i
  dial = (line[0] == 'L' ? dial - spins : dial + spins) % 100
  count += 1 if dial == 0
end
puts count
